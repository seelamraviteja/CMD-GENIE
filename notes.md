# My notes

Personal facts the genie answers from — infra, IPs, hostnames, accounts, URLs.
Edit these from the **📝 My data** panel in the app (or here directly).

If you ask about something that is NOT written here, the genie will say it's
**not available** — it will never guess an IP, host, or credential.

## infra
- Jumpbox IP: 10.12.0.5
- Staging URL: https://staging.example.internal
- Prod DB host: db-prod-01.internal

## accounts
- Docker registry: registry.example.internal (login with the `ci` service account)
